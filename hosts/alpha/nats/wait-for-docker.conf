# /etc/systemd/system/nats-server.service.d/wait-for-docker.conf
# Not strictly required while NATS binds 0.0.0.0, but kept for parity
# with Postgres/Redis in case the bind ever narrows to gateway IPs.
[Unit]
After=docker.service
Wants=docker.service
